<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <link rel="stylesheet" href="app/assets/stylesheet/application.css">
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>The list of articles</h1>

    <%= link_to 'Create an article', new_article_path %>

    <% @articles.each do |article| %>
      <ul>
        <li>
          ID: <%= article.id %>
          <br>
          Title: <%= article.title %> <br> Description: <%= article.description %>
          <br>
          Created: <%= article.created_at %>, Updated: <%= article.updated_at %>
          <%= link_to 'Show', article_path(article) %>
          <%= link_to 'Edit', edit_article_path(article) %>
          <%= link_to 'Delete', article_path(article), method: :delete, data: {confirm: "Are you sure?"} %>
        </li>
      </ul>

    <% end %>

  </body>
</html>
